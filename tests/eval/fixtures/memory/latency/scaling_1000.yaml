version: "1"
scenario_id: "latency-scaling-1000"
description: "Latency baseline at 1000 generated records."
eval_dimensions:
  - "latency_regression"
setup:
  generated_records:
    - scope: "agent:eval-agent"
      type: "fact"
      count: 1000
      content_prefix: "Generated latency fact"
      title_prefix: "Latency fact"
sessions:
  - id: "query-session"
    turns:
      - user: "what does memory know about latency?"
        assistant:
